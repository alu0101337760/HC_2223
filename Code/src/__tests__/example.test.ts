// Example test. Run with `npm test`

describe("Example", () => {
  test("Should work", () => {
    expect(true).toBe(true);
  });

  test("Should fail", () => {
    expect(true).toBe(false);
  });
});
